// import { getPost, getPosts } from "@/actions/posts";
// import { PostParams } from "@/app/types/blog.types";
// import { useQuery } from "@tanstack/react-query";

// export const useGetPosts = (params:PostParams) =>
//   useQuery({queryKey:['posts'], queryFn: () => getPosts(params.page, params.searchQuery)});

// export const useGetPost = (postId: string) =>
//   useQuery({queryKey: ['post', postId], queryFn: () => getPost(postId)});